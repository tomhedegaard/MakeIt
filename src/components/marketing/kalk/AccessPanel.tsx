/** Kalk access-panel stub (spec 2026-09-17 §5). Filled in a later task. */
export default function AccessPanel() {
  return (
    <section id="waitlist" aria-labelledby="waitlist-heading">
      <h2 id="waitlist-heading">Access Panel</h2>
    </section>
  );
}
