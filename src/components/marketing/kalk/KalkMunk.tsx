/** Kalk munk stub (spec 2026-09-17 §5). Filled in a later task. */
export default function KalkMunk() {
  return (
    <section id="munk" aria-labelledby="munk-heading">
      <h2 id="munk-heading">Munk</h2>
    </section>
  );
}
