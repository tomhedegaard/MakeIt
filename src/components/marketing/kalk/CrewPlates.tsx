/** Kalk crew-plates stub (spec 2026-09-17 §5). Filled in a later task. */
export default function CrewPlates() {
  return (
    <section id="crew" aria-labelledby="crew-heading">
      <span id="tiers" />
      <h2 id="crew-heading">Crew Plates</h2>
    </section>
  );
}
